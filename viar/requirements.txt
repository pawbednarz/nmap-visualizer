# Core framework
fastapi==0.115.0
uvicorn[standard]==0.31.0
pydantic==2.9.0
pydantic-settings==2.5.2

# LLM orchestration
langchain==0.3.0
langchain-anthropic==0.2.0
langchain-openai==0.2.0
langgraph==0.2.0

# HTTP client (JIRA integration)
httpx==0.27.0

# Video processing
opencv-python-headless==4.10.0.84
numpy==2.1.0
Pillow==10.4.0

# PDF export
weasyprint==62.3

# Markdown processing
markdown==3.7

# Security (PII scrubbing)
# All built-in to standard library

# XML parsing (already in stdlib, but keeping explicit)
# lxml is faster for large Burp files
lxml==5.3.0

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.0  # for TestClient
